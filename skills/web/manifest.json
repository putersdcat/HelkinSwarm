{
  "domain": "web",
  "version": "1.0",
  "tools": [
    {
      "name": "web_search",
      "description": "Search the web for current information. Returns titles, URLs, and text snippets for the top results. Use when the user asks about recent events, wants to look something up online, or needs information you don't already know.",
      "risk": "low",
      "dataSensitivity": "non-pii",
      "allowedModelLane": "any",
      "requiresConfirmation": false,
      "requiresExecutor": false,
      "externalAutomationCapabilities": [],
      "longTermMemorySchema": [],
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query (e.g. 'SpaceX Starship launch 2026', 'best TypeScript ORM')"
          },
          "count": {
            "type": "number",
            "description": "Number of results to return (1-10, default: 5)"
          },
          "market": {
            "type": "string",
            "description": "Country code for localized results (e.g. 'us', 'nl'). Default: 'us'"
          }
        },
        "required": [
          "query"
        ]
      },
      "aliases": [
        "web lookup",
        "internet search"
      ],
      "discoveryTerms": [
        "current information",
        "recent news",
        "online search"
      ],
      "useWhen": [
        "the user needs fresh public-web information that is not already in the local skill set"
      ],
      "typicalInputs": [
        "latest Azure OpenAI pricing",
        "SpaceX Starship launch 2026"
      ],
      "returnsSummaryShape": "top web search results with titles, urls, and snippets",
      "privilegeClass": "read-only",
      "requiresSubAgent": false
    }
  ],
  "shortName": "web",
  "displayName": "Web Search",
  "shortDescription": "Search the web via Brave Search API (when key is configured) or DuckDuckGo Instant Answer (zero-credential fallback)",
  "discoveryHints": [
    "web search",
    "fresh public information",
    "news",
    "research"
  ],
  "orchestratorUseCases": [
    "look up recent public information on the open web"
  ],
  "recommendedEntryTools": [
    "web_search"
  ],
  "iconUrl": "https://helkinswarmtabsst.z20.web.core.windows.net/icons/web.png",
  "deploymentScenario": "personal-user-centric",
  "onboardingMethod": "automatic-agentic",
  "lifecycleRules": "keep-credentials",
  "externalAccountsNeeded": [
    {
      "description": "Brave Search API key (optional — skill falls back to DuckDuckGo Instant Answer when absent)",
      "envVarName": "BRAVE_SEARCH_API_KEY",
      "howToObtain": "Free tier at https://api.search.brave.com/; store key in stamp Key Vault as BraveSearchApiKey for full-quality results",
      "satisfiedBy": "operator-kv",
      "required": false
    }
  ],
  "maintenanceTasks": [
    {
      "name": "verify-brave-api-key",
      "type": "scheduled",
      "schedule": "0 0 0 * * 1",
      "description": "Verify Brave Search API key is valid by making a test query"
    }
  ]
}
