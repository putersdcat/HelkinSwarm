fastapi==0.115.12
uvicorn[standard]==0.34.2

# --- Core numerical / scientific ---
numpy==2.2.4
pandas==2.2.3
scipy==1.15.2
sympy==1.13.3
mpmath==1.3.0
statsmodels==0.14.4
networkx==3.4.2

# --- Plotting ---
matplotlib==3.10.1
seaborn==0.13.2
plotly==5.24.1

# --- Utility / general-purpose (#674) ---
tqdm==4.67.1
requests==2.32.3
ecdsa==0.19.0

# --- Optimization (#674) ---
PuLP==2.9.0

# --- Astronomy / physics (#674) ---
astropy==7.0.1
qutip==5.0.4
control==0.10.1

# --- Biology / chemistry pure-python set (#674; rdkit/pyscf deferred) ---
biopython==1.84
pubchempy==1.0.4
dendropy==5.0.1

# --- Games / media pure-python set (#674; pygame/polygon deferred) ---
chess==1.11.1
mido==1.3.3
MIDIUtil==1.2.1

# -----------------------------------------------------------------------------
# Honestly deferred libraries (#674 — surfaced in /health inventory):
#   - rdkit         requires Boost + curated wheels, material image-size impact
#   - pyscf         requires gfortran/BLAS build chain
#   - pygame        requires SDL2 system libraries
#   - polygon       ambiguous package name on PyPI; not part of stable wheel set
#   - torch         ~2GB image bloat; not justified for current swarm tasks
#   - python-snappy requires libsnappy-dev apt package
# These are reported as missing in the /health endpoint inventory so agents
# know what is and isn't available, rather than failing silently at import.
# -----------------------------------------------------------------------------
