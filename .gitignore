# Planning Document Snapshot Rollups
docs/Archive/

# Local Only Scratch Files
LocalOnly/
tmpJunk/
infra/graph-permissions.json

# Dependencies
node_modules/

# Build output
dist/

# Environment files
.env
.env.local
.env.*.local

# Azure Functions
local.settings.json
.azure/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/

# Coverage
coverage/

# Teams app packages (built locally)
appPackage/*.zip

# Temp/scratch files
tmp-*
appid.txt

# python cache
.mypy_cache

# User-map is source-controlled — it contains only routing metadata (alias, endpoint, UPN).
# No credentials or secrets. See config/user-map.json.
# config/user-map.json

# pnpm lockfile is committed, but store is local
.pnpm-store/

# Archival research — v0 lineage analysis (isolated, never to poison v2)
ArchivalResearch/

# Compiled ARM templates (generated from Bicep)
infra/main.json
# MCP Teams Test Harness — local auth cache and discovered chat settings
.local/
.vscode/mcp-settings.json
dist-mcp/

# Playwright test artifacts
.playwright-mcp/
open-issues.json
tok-check.json

# All the graphify shit
src/graphify-out/
/graphify-out/

# Generated outputs — recoverable from git history or re-runnable scripts
*-dossier.md
*-bundle.md
*_export*.json
project_sitrep_*.md
sitrep_cloc_*.txt
clean-docs-manifest*.md

# Azure Functions host key (secret-adjacent, never commit)
.hostkey.tmp

# Historical docs (deleted, prevent re-accumulation)
docs/ChatLogs/
docs/RandoStuff/
docs/Delivery/
docs/RestartFromZero_Or_NotToBe_That_Is_The_Questions/
docs/OtherToolComparisons/
ResearchDocs/

# Extensions — source is tracked, build artifacts are not
extensions/*/node_modules/
extensions/*/out/